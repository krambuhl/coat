{!import heading './heading.coat'}

{!extends './card.coat'}

{@card-header}
  <header class="card-header">
    <img src="{src}" alt="{alt}" />
    <heading level="3">{>contents}</heading>
  </header>
{/card-header}
